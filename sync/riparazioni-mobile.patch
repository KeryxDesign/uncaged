--- sync/_design-al-2026-09-04.html	2026-09-05 16:46:10
+++ index.html	2026-09-04 09:14:43
@@ -11,6 +11,13 @@
 <link rel="stylesheet" href="_ds/keryx-design-system-1e4ff7df-396a-4879-bdfb-75be3456348c/styles.css">
 <script src="_ds/keryx-design-system-1e4ff7df-396a-4879-bdfb-75be3456348c/_ds_bundle.js"></script>
 <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
+<title>UNCAGED</title>
+<link rel="manifest" href="./manifest.webmanifest">
+<meta name="theme-color" content="#031a2b">
+<meta name="mobile-web-app-capable" content="yes">
+<meta name="apple-mobile-web-app-capable" content="yes">
+<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
+<meta name="apple-mobile-web-app-title" content="UNCAGED">
 <style>
 html,body{margin:0;height:100%;background:hsl(204 96% 6%);overscroll-behavior:none;-webkit-text-size-adjust:100%}
 button{touch-action:manipulation}
@@ -537,7 +544,6 @@
                 <div style="font-family:'Playfair Display',Georgia,serif;font-weight:800;font-size:17px;line-height:1.15;color:hsl(42 88% 70%)">{{ selPrigioniero }}</div>
                 <div style="font-size:10.5px;letter-spacing:.03em;white-space:nowrap;color:hsl(40 30% 90% / .55)">{{ selEtichetta }}</div>
               </div>
-              <div style="display:inline-flex;align-self:flex-start;margin-top:7px;padding:3px 9px;border-radius:999px;background:hsl(199 60% 40% / .35);border:1px solid hsl(199 70% 60% / .4);font-size:10px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;color:hsl(199 85% 82%)">{{ selGenere }}</div>
               <div style="font-size:12px;line-height:1.45;color:hsl(40 30% 90% / .78);margin-top:7px;text-wrap:pretty">{{ selRitratto }}</div>
               <div style="font-size:11.5px;line-height:1.5;color:hsl(40 30% 90% / .62);margin-top:5px;text-wrap:pretty">{{ selLore }}</div>
               <div style="font-family:'Libre Baskerville',Georgia,serif;font-style:italic;font-size:12px;line-height:1.5;color:hsl(42 88% 76%);margin-top:8px;padding-left:10px;border-left:2px solid hsl(42 87% 55% / .5);text-wrap:pretty">{{ selBattuta }}</div>
@@ -697,7 +703,7 @@
       </sc-if>
 
       <sc-if value="{{ aRisultato }}">
-      <div style="position:absolute;inset:0;display:flex;align-items:center;background:hsl(204 96% 10%);color:hsl(40 30% 90%);padding:14px 46px;gap:40px;overflow:auto">
+      <div style="position:absolute;inset:0;display:flex;flex-wrap:wrap;align-items:center;background:hsl(204 96% 10%);color:hsl(40 30% 90%);padding:14px 46px;gap:40px;overflow:auto">
         <div style="position:absolute;left:120px;top:50%;width:180px;height:180px;border-radius:50%;border:2px solid hsl(42 87% 55% / .5);transform:translate(-50%,-50%);animation:{{ animBurst }};pointer-events:none"></div>
         <div style="flex:1;min-width:0;min-height:0;animation:{{ animRisultato }}">
           <div style="font-size:12px;font-weight:700;letter-spacing:.12em;text-transform:uppercase;color:hsl(42 87% 55%)">{{ urloTaglio }}</div>
@@ -712,7 +718,7 @@
             <button type="button" onClick="{{ chiudiRisultato }}" style="min-height:50px;padding:0 22px;border-radius:14px;border:1px solid hsl(40 30% 90% / .2);background:transparent;color:hsl(40 30% 90%);font-family:inherit;font-weight:700;font-size:15px;cursor:pointer">{{ tChiudi }}</button>
           </div>
         </div>
-        <div style="width:360px;display:flex;flex-direction:column;gap:12px">
+        <div style="width:360px;max-width:100%;display:flex;flex-direction:column;gap:12px">
           <div style="display:flex;gap:12px">
             <div style="flex:1;padding:18px;border-radius:16px;background:hsl(204 96% 6%);border:1px solid hsl(40 30% 90% / .1)">
               <div style="font-family:'Playfair Display',Georgia,serif;font-size:36px;font-weight:800;line-height:1;color:hsl(42 87% 55%)">{{ punti }}</div>
@@ -1836,9 +1842,29 @@
     carte.length = this.nCarte;
     this.iPrimaManico = carte.findIndex(c=>!c.teoria);
     this.pulisci();
+    this.schermoIntero(true);
     this.setState({schermo:'gioco', carte, indice:0, combo:0, punti:0, perfette:0, onde:[], tempiGiro:[], mediaGiro:0, aiutiGiro:0}, ()=>this.avviaCarta(0));
   };
 
+  // le barre del browser rubano altezza proprio al manico. Si chiede lo schermo
+  // intero sul gesto di chi gioca, l'unico momento in cui il browser lo concede.
+  // Su iPhone l'API non esiste: li' l'unica strada e' "Aggiungi a Home".
+  schermoIntero(entra){
+    try{
+      if(entra){
+        const el = document.documentElement;
+        const req = el.requestFullscreen || el.webkitRequestFullscreen;
+        // il browser puo' rifiutare: senza .catch il rifiuto finisce in console
+        if(req && !document.fullscreenElement){ const r = req.call(el, {navigationUI:'hide'}); if(r && r.catch) r.catch(()=>{}); }
+        if(screen.orientation && screen.orientation.lock) screen.orientation.lock('landscape').catch(()=>{});
+      } else {
+        if(screen.orientation && screen.orientation.unlock) screen.orientation.unlock();
+        const esci = document.exitFullscreen || document.webkitExitFullscreen;
+        if(esci && document.fullscreenElement){ const r = esci.call(document); if(r && r.catch) r.catch(()=>{}); }
+      }
+    }catch(e){}
+  }
+
   avviaCarta(i){
     // i timer appartengono alla carta che li ha programmati: spegnerli qui, sempre,
     // è la sola garanzia che nessuno ri-timbri this.avvio a carta già avviata
@@ -1986,8 +2012,13 @@
     for(let i=0;i<buf.length;i++) somma += buf[i]*buf[i];
     const rms = Math.sqrt(somma / buf.length);
     if(rms < 0.012) return -1;
+    // il taglio e' relativo al picco del buffer: con una soglia fissa a 0.2 il
+    // microfono era sordo sotto quel livello, e una chitarra piana o una stanza
+    // grande ci finivano quasi sempre
+    let picco = 0;
+    for(let i=0;i<buf.length;i++){ const v = Math.abs(buf[i]); if(v > picco) picco = v; }
     let inizio = 0, fine = buf.length - 1;
-    const soglia = 0.2;
+    const soglia = Math.max(0.02, picco * 0.2);
     while(inizio < buf.length/2 && Math.abs(buf[inizio]) < soglia) inizio++;
     while(fine > buf.length/2 && Math.abs(buf[fine]) < soglia) fine--;
     const b = buf.slice(inizio, fine);
@@ -1999,7 +2030,12 @@
     while(d < n-1 && c[d] > c[d+1]) d++;
     let max = -1, pos = -1;
     for(let i=d;i<n;i++) if(c[i] > max){ max = c[i]; pos = i; }
-    if(pos <= 0) return -1;
+    if(pos <= 0 || !c[0]) return -1;
+    // quanto il segnale e' davvero periodico: una nota sta sopra 0.94, il rumore
+    // sotto 0.39. Senza questo controllo la soglia relativa sentirebbe note
+    // dentro il fruscio della stanza
+    const chiarezza = (max / (n - pos)) / (c[0] / n);
+    if(chiarezza < 0.6) return -1;
     // interpolazione parabolica per affinare il periodo
     const x1 = c[pos-1] || 0, x2 = c[pos], x3 = c[pos+1] || 0;
     const a = (x1 + x3 - 2*x2) / 2, bb = (x3 - x1) / 2;
@@ -2657,7 +2693,6 @@
       selPrigioniero: prig(qSelId, selForma).n,
       selRitratto: ritrattoDi(qSelId, selForma)[st.lingua] || ritrattoDi(qSelId, selForma).it,
       selBattuta: prig(qSelId, selForma)[st.lingua] || prig(qSelId, selForma).it,
-      selGenere: loreDi(qSelId, selForma).g[st.lingua] || loreDi(qSelId, selForma).g.it,
       selLore: loreDi(qSelId, selForma)[st.lingua] || loreDi(qSelId, selForma).it,
       selEtichetta: T.formaCaged + ' ' + selForma + ' · ' + (pctForma[selForma] >= 90 ? T.liberato : T.ingabbiato(100 - pctForma[selForma])),
       liberato: mediaPadr >= 90,
@@ -2717,7 +2752,7 @@
       riprendiGiro: this.nuovoGiro,
       chiudiDeck: this.chiudiDeck,
       tFineDeck: (mazzo === 'tutorial' && st.tutorialPrima) ? T.iniziaOra : T.chiudiStoria,
-      tornaHome: () => { this.pulisci(); this.setState({schermo:'profilo', fase:'attesa'}); },
+      tornaHome: () => { this.pulisci(); this.schermoIntero(false); this.setState({schermo:'profilo', fase:'attesa'}); },
       pips, contatoreCarte: (st.indice+1) + '/' + this.nCarte,
       combo: st.combo, punti: st.punti, perfette,
       comboBg: st.combo > 0 ? 'hsl(42 87% 55% / .16)' : 'hsl(40 30% 90% / .07)',
